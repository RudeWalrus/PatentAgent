source "http://rubygems.org"

# Specify your gem's dependencies in patentagent.gemspec
gemspec

group :development do
  gem 'rb-fsevent', require: false if RUBY_PLATFORM =~ /darwin/i
  gem 'guard-rspec'
end
